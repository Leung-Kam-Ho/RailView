export type Train = {
  id: string;
};

export type Coach = {
  id: number;
  type: string;
};

export type Wheel = {
  id: string;
};

export type WearDataPoint = {
  time: number;
  level: number;
};
